public class Primitivedata_Assignment {
    public void primitiveDataAssignment(){
        ID accountID = '0018d00000PmCDnAAN';
        String strID = 'The following Id belongs to Accounts';
        Boolean blnID = true;
        system.debug('ID is ' + accountID + '. ' + strID + '==> ' + blnID);
    }
}